# Pokémon TCG Pocket data

The JSON files in this directory are generated from
`kelin132/pokemon-tcg-pocket-database`, which is based on the
`flibustier/pokemon-tcg-pocket-database` dataset.

The card images are served from the public `pokemon-tcg-exchange` image
repository at runtime. The bot stores card metadata and image URLs, not image
files.