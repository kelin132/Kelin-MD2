# Dependencies
node_modules/

# WhatsApp session — NEVER commit these (contains auth keys)
sessions/

# Runtime data
backups/
*.log
*.log.*

# Environment secrets
.env

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Replit workspace files (not needed for bot deployment) ──
.replit
.replitignore
.npmrc
replit.md
pnpm-workspace.yaml
pnpm-lock.yaml
tsconfig.json
tsconfig.base.json
.agents/
.local/

# Dashboard & API server (Replit-only, not needed on hosting panels)
artifacts/

# Shared TypeScript libs (Replit-only)
lib/api-client-react/
lib/api-spec/
lib/api-zod/
lib/db/

# Build scripts (Replit-only)
scripts/
